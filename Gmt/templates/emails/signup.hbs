<h3>New User</h1>
	{{#if mailBody.email}}

		<p class="text-larger">From
			{{#if mailBody.name}}
				<strong>{{mailBody.name}}</strong>
			{{/if}}
		</p>
		{{#if mailBody.email}}
			<a href="mailto:{{enquiry.email}}">Email: {{mailBody.email}}</a>
		{{/if}}
		Click on link below and login Again to veryfy 
		<a href="188.166.178.107/verify"> VERIFY </a>
	{{/if}}
